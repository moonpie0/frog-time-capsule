# 纪念档案 Viewer UI 标准

本规范用于本仓库中需要选择素材、组合图层或导出图片的页面。现有“不倒翁”和“明信片 > 常规”是基准实现；以后新增博物馆、换装或其他拼装页面时沿用同一结构和交互。

## 页面结构

- 笔记列表标题显示“已拥有数/总数”：见闻为 112/137，旅友为 27/27，从已核对的个人记录统计。搜索、框样式筛选、排序和两套布局切换不改变这组收藏总数。
- 主导航使用“相册”，内部为“特殊、目的地、常规、动态照片”。保留已有 `postcards` 路由兼容旧链接。
- “笔记”主导航紧跟“故事”。内部为“见闻、旅友”，使用 `note_info_1/2_png`、`note_friend_1/2_png` 原始选中与未选中按钮图；通用配置含 137 种见闻、27 种旅友及 27 段附加笔记。连续向下展示，搜索、框样式筛选和排序放工具栏右侧。默认日期从新到旧，可切换日期从旧到新或编号从小到大；两种日期排序都把无日期条目放在最后，同日按编号排列。两套布局共用排序，切换分类或布局保留排序条件，详情上一篇／下一篇遵循当前列表顺序。
- 笔记与旅行趣事一样，每次打开或刷新默认“简洁收藏”，右侧单按钮切换“游戏界面／简洁收藏”。简洁收藏采用图鉴大小的方形图片网格与 12px 留白；游戏界面采用原框、符号文字和附加笔记连续列表。两套共用分类、搜索、框样式筛选与详情，切换保留条件，不读取旧布局偏好。
- 笔记简洁收藏卡片在方形配图下方向下延伸，编号后直接展示对应青蛙文字，按原始符号顺序和分行排列。旅友附加内容在同卡片下方以细线分隔，展示小配图、附加编号和符号文字；不嵌套卡片。图片保持原比例，文字区域自适应卡片宽度，不能裁掉最后一行或溢出边框。
- 笔记使用原配图、青蛙符号及 `note_quality_1/2_png` 普通／金色框，下方附加内容使用 `note_add_png`。原配置、197 个符号的逐行顺序与间距、配图哈希保存在 `parsed/notes-catalog.json`，源配置存于 `parsed/notes-source/`。组合预览独立生成，不改原图。符号按原 `TravelNoteUtils` 的宽 40／20 和行距 50 排列，不猜测中文翻译。
- 笔记的 `quality` 决定框样式，不代表账号拥有；`attach` 决定附加内容的对应关系，不代表已经获得。用户截图的汇总数量不能直接用于推断每一条。页面底部提供“图片、青蛙文字与界面素材”入口。
- 笔记个人日期独立保存于 `parsed/notes-dates.json`，不改原始目录或游戏配置。已从 28 张见闻截图和 9 张旅友截图逐图核对 112 条见闻、27 条旅友的日期与编号，保存分类、截图文件名、行号与 SHA-256；原始截图留在外层本地核对目录。日期是游戏条目显示日期，不使用截图或文件创建时间替代。简洁收藏在主笔记青蛙文字下显示日期，游戏界面在原框右下角显示日期，详情共用同一记录。附加笔记没有独立日期，不复制主笔记日期或重复统计；其他 25 种见闻日期留空，不能仅因本次截图未包含而推断未拥有。重新生成保留个人日期，页面底部提供按见闻、旅友分别汇总的日期清单。
- 原“道具资料、贴纸分享、活动图片、旅行汇总、其他图片、全部图片”主导航合并为“分享图片”。内部依次为“明信片、贴纸、道具、旅行汇总、抓拍”，其中抓拍对应原“角色插画”，展示名称同步改为抓拍，原始元数据保留。明信片使用原有分享成品图，不混入相册拼装资源。不倒翁收藏与补充截图保留独立入口。
- 分享图片中尚未确认归属的 7 张活动图、4 张收藏分享和 1 张扭蛋记录暂放“其他”子栏，待本人确认。子栏计数按唯一原件统计；复用横向标签和右侧搜索、年份、排序，切换子栏保留搜索和年份、清除不适用的明信片类型筛选。旧分类链接和原图直达链接继续可用，重新生成不修改原图或来源记录。
- 动态照片使用横向场景子栏、固定 10:7 预览和内嵌播放进度；暂停、从头播放使用带提示的图标按钮，“保存 GIF”放在工具栏右侧，导出期间切换为取消按钮并显示进度。它是动画播放器，不提供未经配置支持的图层位置编辑。
- 动态照片播放器和素材完全本地化，进入子栏时按需载入，离开相册或页面不可见时停止播放。使用 Spine 时间轴与 `layer` 顺序，GIF 按 500 × 350、10 帧/秒保存完整的原速循环，与当前预览进度、倍速无关。编码在本地 Worker 中进行，不上传数据；原始素材不转码或覆盖。
- 动态照片获得情况单独保存在 `parsed/dynamic-photo-ownership.json`，只有油菜花（动态 ID 2）已拥有，其余 5 组未拥有。所有场景继续展示，仅已拥有场景在子栏与播放状态区显示绿色“已拥有”（与图鉴一致，`#21674d`）；未拥有场景不显示状态文字。重新生成不得用静态素材存在与否覆盖本人确认，导出提示也不得重新附加“未拥有”文字。

1. 页面顶部放标题与一行简短统计，拼装操作集中在右侧素材选择区。
2. “随机组合”和“保存 PNG”放在素材分类栏的最右端，与“场景、角色、背景、前景”或不倒翁部件分类同一行。宽度不足时按钮组整体换行、右对齐，不遮挡分类。手动调整不设置独立按钮。
3. 一级或二级分类紧接标题区域，以横向、可换行标签展示。选中项使用浅绿色背景和底部深绿色指示线。
4. 编辑区采用左右两栏：左侧为固定比例预览，右侧为素材选择。不得把画布、选择器和说明堆叠成多张嵌套卡片。
5. 画布下方依次放撤销、重做、复位三个图标按钮。状态文字放在按钮下方，保持一行或自然换行。
6. 右侧先放“场景、角色、背景、前景”等编辑标签，再放缩略图选项。选择可调整的图层后，滑块直接显示在选项区下方。
7. 分类与命令共用 `.composer-toolbar`，命令组共用 `.composer-actions`；命令必须在 tablist 外，避免被当作分类标签。目的地的地区筛选也使用上方可换行子栏。

## 搜索与筛选

- 主导航“故事”紧跟“图鉴”，内部为“旅行趣事、节日趣闻”。选中的按钮使用 `stroy_travel_on_png` 或 `stroy_special_on_png`，未选中时使用对应的 `stroy_travel_off_png` 或 `stroy_special_off_png`；点击、刷新和哈希跳转时同步图片与选中状态。保留图片原有文字与底部选中指示线。“界面素材”链接放在两个子栏内容之后的故事页底部。
- 节日趣闻首先分为“收藏、自由组合”，不再设置春节／中秋节一级切换。收藏统一展示本人确认的记录。自由组合的底图固定分两行：第一行中秋节（荷塘月色、阁楼赏月、桂花树下、屋檐天灯、中秋夜游），第二行春节（新春赐福、年味饺子、萌兔舞狮、兔年喜柿、元宵花灯）；窄屏时各行内部可换行。两行之间不画横线，仅在整个底图区域下方保留分隔线。点击任意底图直接切换编辑器，两行总共只有一个选中项。
- 中秋节有 25 张贴纸，春节有 12 张贴纸；每张底图有三个独立贴纸位，每个位可从该节日全部贴纸中选择或清空。个人收藏与配置素材分开，不将自由组合标为收藏。春节账号收藏尚未补充，不推断未拥有。
- 春节原图和配置从本地备份中的静态资源恢复，17 张 PNG 原样保存于 `media/stories/spring/`，原始配置为 `parsed/spring-card-config.json`，关联与 SHA-256 在 `parsed/spring-greetings.json`。选中春节底图时，编辑器最底部以小字说明：这套贺卡原属游戏的春节活动，并不在游戏内的“故事 > 节日趣闻”中，在此一并归档展示。
- 两个节日复用同一编辑器实现，但素材、撤销历史和本地存储隔离；切换保留各自的底图与贴纸位置。收藏与自由组合切换后返回当前底图。中秋节继续使用原有本地存储键，不清除旧组合。春节的初始位置、固定大小取 `SpringCardUtils.createCard` 使用的 `pos`、`style`；自由拖动是本地编辑器功能，不声称游戏活动原本支持任意拖动。
- 贺卡编辑器复用不倒翁两栏布局、缩略图、内嵌横向／纵向滑块、翻转和撤销／重做／复位，支持画布拖动。大小固定为各贴纸位的原始 `style`，旋转固定为 0，不提供这两项控件；旧浏览器组合迁移时保留位置和翻转，清除缩放和旋转偏移，导出和历史恢复也遵守限制。底图前后景按原始 `layer` 顺序绘制，贴纸初始位置取各底图的 `pos`。每张底图的组合独立保存在浏览器本地，不写入账号收藏。随机组合与“导出 PNG”放在右侧贴纸分类工具栏末端；画布采用原图 500 × 300 的比例，导出为 1000 × 600，无外部依赖。
- 节日趣闻收藏已按两张本人截图复刻 4 张贺卡，素材、坐标、来源截图与哈希记录在 `parsed/greeting-collection.json`。详情注明截图复刻，提供大图、PNG 下载和来源截图；不得称为取得服务器原图。原始两张截图原样存于“补充截图”。
- 旅行趣事不显示作者署名行。友情绘本取消“场景与界面”子栏，原素材保留在 `viewer/friendship-ui.html`，主页面底部提供“界面素材”链接。
- 旅行趣事每次打开或刷新默认显示“简洁收藏”，不读取旧版显示偏好。搜索栏旁仅保留一个切换按钮：简洁视图显示“游戏界面”，游戏视图显示“简洁收藏”。简洁收藏沿用节日趣闻收藏卡片，展示图片、名称、来自昵称与绿色“已回礼”，点击打开完整正文详情。两种视图共用 25 条数据、按 ID 排序，切换保留搜索条件、无结果状态和账号标注；黄色纸面仅在游戏视图显示。
- 旅行趣事简洁卡片的尺寸与图鉴一致：沿用默认图片网格，图片区为 1:1、白底、12px 留白。节日趣闻收藏沿用相册 `.goal-postcards-gallery` 的网格，桌面最小列宽 230px、窄屏单列；贺卡图片区保留原始 5:3 比例，不拉伸或裁掉内容。
- 旅行趣事按故事 ID 顺序展示 25 种，每种一行、图文左右交错，连续滚动，不分页。黄色纸面使用 CSS `#fff3bf` 铺满旅行趣事列表区域的宽度，不能只显示为中间的窄纸片；图文保持合适阅读宽度。上方页面标题、子栏按钮、搜索工具栏保留原背景。不使用整页黄色背景图；纸面标题和条目纹理使用原始素材。搜索在工具栏右侧。
- “来自”昵称按故事 ID 保存于 `parsed/story-senders.json`，以本人提供的内容为准，重新发布静态素材或生成页面不得覆盖。使用原版绿色印章展示昵称，保留表情和特殊字符；搜索同时匹配昵称。印章独占条目底部空间，避免遮挡正文和插图。
- 静态故事类型与个人收件记录分开保存；没有证据的来源昵称、赠礼和回礼状态不填充。界面素材提供独立预览页，不将静态素材视为账号已获得记录。
- 25 种旅行趣事均已由本人确认回礼，记录于 `parsed/story-senders.json` 的 `returned_gifts`，重新生成时保留。每条左上角使用 `story_state_bg_png` 图钉便签叠加 `after_send_present_png` 实心爱心，悬停与无障碍名称为“已回礼”；这是纪念状态，不是可发送礼物的按钮。

- “特殊”按参考图及用户确认分为旅友合照、旅友独照、固定套图、博物馆、随机套图、城市系列、濒危动物、节日系列、森之国度、路标系列、动植物系列，另设全部和节气。`linkage` 开头的资源归入森之国度，博物馆与固定套图分别展示。分类映射保存在 `parsed/special-postcard-categories.json`，生成时校验每个静态图片 ID 恰好归入一类。
- 特殊图片展示名称按用户参考图整理，映射与依据保存在同一文件的 `titles` 和 `title_source` 中。保留原始资源名、ID、标题与原命名依据；名称同步用于列表、搜索和详情。参考图未覆盖的节气图片保留月份和组别，不推测具体节气。
- 静态分类数量表示已保存图片数；随机套图数量表示可编辑场景数。随机套图复用蓝花藤、橘猫海棠花、绣眼鸟木棉花的现有编辑器及素材，不重复保存图片或增加原件计数。常规入口继续保留；两个入口在页面内各自保留编辑状态与撤销历史。
- 分类切换保留搜索条件，搜索仅作用于当前静态分类。图片详情关闭后回到当前分类；图片直达链接清除不相关筛选并打开对应分类。

- 图片列表参照“不倒翁 > 我的收藏”：搜索框、筛选框和排序框位于图片列表上方的工具栏右侧；有列表标题时标题在左侧，没有标题时也保持控件右对齐。
- 搜索在前，筛选或排序在后。特殊的搜索框和目的地的搜索、角色筛选遵守同一规则，不移入分类标签栏或拼装操作栏。
- 复用全局 `.toolbar`、`input[type=search]`、`select` 的字体、边框、圆角、内边距和聚焦样式。桌面搜索框默认 220px，控件间距 12px；明信片仅通过 `.goal-postcards-toolbar` 设置 `justify-content:flex-end`，不另建输入框皮肤。
- 窄屏复用全局工具栏的弹性宽度与换行规则，搜索、筛选尽可能同行，空间不足再换行；不通过缩小文字或强制超宽维持桌面布局。
- 保留搜索清空、筛选组合、无结果状态和可访问名称。修改后在桌面和 390px 视口检查对齐、无重叠、无横向溢出，并实际搜索和切换筛选。

## 控件与状态

- 称号默认展示全部：名称后仅对有效的已解锁项显示绿色“已解锁”（`#21674d`），过期项显示“已过期”（提醒色 `#936111`），未解锁项不显示状态文字。过期标记优先，不与“已解锁”重复显示；过期仍计入曾解锁，不计入未解锁。本人后续确认保存在 `parsed/titles.json` 的 `confirmations`，重新生成时保留。筛选提供全部、已解锁、未解锁、已过期。
- 隐藏未获得标记仅影响动态照片与称号的条目展示，不删除对应数据、条目或筛选能力；汇总数量和筛选选项仍可使用“未拥有／未解锁”文字。状态标记采用简洁文字，不加“本人确认”后缀。

- 素材选项使用统一缩略图按钮，边框半径 4px。选中项使用深绿色描边和浅绿色底色。
- 每个可调整图层提供横向、纵向、大小、旋转和左右翻转。滑块显示当前数值。
- 只允许横向移动的贴边图层仅显示横向滑块，隐藏并禁用纵向、缩放、旋转及翻转；初始化、换角色、随机、复位、恢复本地组合和导出都遵守相同贴边约束。数量标签统一用名称后的独立小号浅色 `span`，不加括号。
- 依据配置与用户参考图固定的图层不显示调整滑块。带猫海棠的猫、地面和角色固定，随机组合只变化花朵；无猫海棠不套用这一锁定规则。
- 滑块拖动期间实时更新画布，松开后只生成一条撤销记录。
- 场景、角色、图层选择、随机组合、复位和翻转都进入同一撤销历史；新操作会清空重做历史。
- 当前组合保存在浏览器本地存储。存储失败时在画布下方提示，不阻止继续编辑或导出。
- 页面加载或切换素材时禁用导出；当前画布完整渲染后再启用“保存 PNG”。

## 图层与导出

- 画布使用明确的逻辑尺寸和导出尺寸。明信片逻辑尺寸为 500 × 350，导出为 1000 × 700。
- 取得照片最终 layers 时，按记录的逐层 x/y 和顺序绘制。Picture 配置位置到最终画布坐标的换算尚未完整验证，不得宣称已复刻游戏坐标规则。
- 没有最终坐标时，透明边界贴边、居中取景与缩放仅属于人工构图方法。不得将这些方法称为原游戏限制，也不得把角色与承托物分开调整后视为已验证构图。
- 滑块范围是编辑器允许的操作范围，不代表游戏坐标上下界。randomSet=false 仅为配置事实，在未取得实现前不能据此推断每个素材的绝对位置。
- 默认构图只是可编辑起点。缺少服务器随机坐标时，页面必须明确说明它是代表性组合。
- 参与 Canvas 绘制的本地 PNG 在生成 HTML 时转为 Data URL，保证通过 `file://` 双击打开时仍可导出，并避免 Canvas 被本地跨源图片污染。
- 导出保持 PNG，不进行有损压缩。文件名包含类型、场景编号、角色组合和时间。

## 响应式与视觉

- 桌面端预览区保持稳定宽度，选择区占剩余空间；窄屏改为上下排列。
- 固定比例画布使用 `aspect-ratio`，动态内容不得改变画布尺寸或推动布局跳动。
- 颜色沿用 Viewer 的中性灰绿体系：主操作深绿，选中底色浅绿，背景浅灰。不要增加渐变、装饰性大卡片或多套互相冲突的按钮样式。
- 文字标签应完整显示或自然换行，不使用缩小字体适配视口，也不允许横向溢出。

## 验收要求

- 桌面和 390px 宽移动视口均无横向溢出。
- 所有分类、场景和素材均可访问；图片载入失败时显示明确状态。
- 撤销、重做、复位、滑块、翻转、随机组合均实际改变状态并可恢复。
- PNG 导出返回非空文件，尺寸与画布规范一致。
- 边缘型素材不得在画面内部露出明显的矩形裁切边。

## 已校准构图与来源

- 图鉴的纪念品、特产、藏品列表使用原始木色框 `zukan_panel_png`；只有七种月饼（`collection_49` 至 `collection_53`、`collection_66`、`collection_67`）使用桂花绿色框 `zukan_panel2_png`，并置于纪念品最前。月饼之间和其余条目之间仍按资源 ID 排序，不按分享图关联置顶。物品、家具不套用图鉴框。
- 框素材与 SHA-256、月饼映射保存在 `parsed/catalog-frames.json`，PNG 原样保存在 `media/catalog-ui/`，生成页面时校验。列表按原 `CollectionItem` 的 130 方框、74 方形物品和向下 5 的中心偏移等比例展示；框是装饰层，不修改原物品图片，详情与下载保持无框原图。卡片名称、获得情况、搜索与筛选保持原有行为。

- 图鉴名称、地区和短说明由 `parsed/catalog-texts.json` 按原配置的图片索引精确关联；原始记录及来源表哈希一并保存，不用配置 ID 猜测图片 ID。列表展示名称与地区，短说明放在详情；博物馆长介绍折叠展示。本人确认的分类、拥有状态和图片关联保持独立。绘本藏品共用相同文字。
- 称号保持纯文字列表，可筛选已解锁、未解锁和全部，搜索同时匹配名称、解锁条件和描述；展开称号显示条件与描述。`parsed/title-catalog.json` 的状态来自本人确认的完整截图清单与静态配置比对，表示截图记录日期的情况，不作为实时账号查询结果。

- 目的地区域标签统一为“台湾·新北”和“台湾·垦丁”，筛选栏和详情使用同一显示名称，内部地区 key 不变。
- 固定套图中的枫叶 3061、鸳鸯 3085 各展示四张组合图。配置中的旅友贴图已经包含青蛙，因此单人版使用 `_qw`，同行版只叠加 `_bh`、`_cw` 或 `_yhc`，不重复添加青蛙。两张底图原样保留，路径和 SHA-256 记入记录的 `background` 字段，生成 Viewer 时一并校验，底图不进入展示列表。

- `parsed/normal-postcard-layouts.json` 单独保存人工构图预设；`parsed/goal-postcards.json` 中的原始 Picture 坐标不改写。预设用于初始化、切换角色、缩略图、随机组合、复位与 PNG 导出。
- 蓝花藤（201）：按可见脚底校准青蛙和刺猬；壁虎贴在栏杆正面。图片透明留白和阴影不能当作脚底位置。
- 特殊站桩蓝花藤为 `Unique` 3201（`back_u_lanhuateng`），使用 `lanhuateng_rail_ts`；普通 201 使用 `lanhuateng_rail_pt`。随机套图入口只使用 3201，普通版继续保留。站桩栏杆以 500 × 350 原尺寸铺满画布；角色采用原配置，固定栏杆和角色，随机只改变前后花藤的水平取景。花藤贴住画布顶边，仅提供横向滑块。青蛙原配置 `(75,28)`，壁虎 `(-153,-84)`，刺猬 `(2,-78)`，萤火虫 `(-52,91)`；花藤范围属于编辑器预设，不作为游戏随机坐标规则。
- 黄花、紫花风铃木（206、207）：校准青蛙、壁虎、刺猬与树枝的接触位置；黄花的承托枝条与角色一起上移，避免角色被画布底部裁掉。飞行中的萤火虫不强制落地。
- 无猫垂丝海棠为 `Normal` 类的 202、203、204，组合配置位置依次为 `(0,-94)`、`(-130,-94)`、`(130,-94)`。按用户确认，包含木桩和角色的完整图层以可见底边贴住画布底边，只允许整体横向移动。随机组合在这三个场景间切换，不跳到猫版；横向滑块范围属于工具设置。
- 竹林 107、108 的 `bamboo_back*`、`bamboo_mid*`、`bamboo_front*` 竹竿以可见顶部贴住画布顶边，只允许横向移动；竹笋、地面和角色不套用竹竿的规则。贴边规则单独保存在 `parsed/normal-postcard-layouts.json` 的 `anchors` 中，原始配置坐标不改写。
- 猫版已在 `Unique` 类找到真实配置 3202（`back_u_chuisihaitang`），之前仅筛选 `Normal` 而漏掉。青蛙配置 `(-10,-4)`，壁虎 `(-55,-33)`，刺猬 `(-22,-95)`，萤火虫 `(-92,103)`。以原始角色坐标和参考图校准构图，最终服务器逐层坐标仍未取得。
- `chuisihaitang_mid.png` 的上部花朵与下部猫身在 Canvas 绘制时分别取 `[0,0,360,112]`、`[0,174,360,176]` 区域。猫身位于前方地面，先画猫再画角色；固定猫和角色，只允许花朵变化。原图文件不裁改，不额外复制整张素材。
- 木棉花绣眼鸟为 `Unique` 类 3203–3205，各自对应 `mumianhua_xyn_mid1/2/3` 和 `mumianhua_xyn_front1/2/3`，保留树干与鸟作为整体，树干和角色固定，前景可以调整。原始配置、资源 ID 与使用的素材映射保存在补充配置中。
- 配置保留 19 类、39 个 Normal 场景和 256 张原始素材，另外展示 5 个 Unique 补充场景；都不等同于账号拥有记录。
- 预设版本升级通常保留用户滑块偏移。旧 `begonia-cat` 状态映射到真实 ID 3202，并重建错误构图，清除旧猫和角色的位移；其他常规场景的用户调整继续保留。
- 版本 3 为无猫海棠和竹竿新增贴边约束：已有组合保留横向位移，清除这些图层的纵向、缩放、旋转和翻转偏移；猫版与其他未涉及图层的调整保留。
