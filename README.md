# 旅行青蛙 · 我的游戏账号纪念档案

这是我在《旅行青蛙·中国之旅》中留下的个人游戏记录。为了在停服前尽可能保留这段游玩经历，我将自己手机中能够读取的明信片、分享图片和图片上的文字整理成一份可以离线浏览的纪念档案。

档案以我的游戏图片为主体，另附从游戏资源中整理出的通用素材图鉴。它保存的是目前能够取得的图片和信息，属于**部分数据存档**，不能用于恢复账号、还原完整游戏进度或继续游玩。

## 查看档案

- 在线浏览：[我的旅行青蛙时光胶囊](https://moonpie0.github.io/frog-time-capsule/)
- 本地浏览：打开 [viewer/index.html](viewer/index.html)，无需安装软件或启动服务器。

页面可以查看明信片、图鉴与收藏、图鉴、物品、家具、道具资料、贴纸分享、活动图片、旅行汇总和个人资料，并支持图片搜索、筛选、放大预览及关联图片查看。“图鉴、物品、家具”为左侧同级入口；图鉴内分纪念品、特产、藏品，物品暂不细分。左侧“不倒翁”提供收藏与部件拼装体验。本地文件修改后，需要同步到 GitHub 并完成 Pages 部署，在线页面才会更新。

离线保存时请下载整个仓库，保留目录结构。只保存 HTML 文件会缺少图片。

本地 Git 仓库位于 `D:\ly\旅行青蛙\GameTimeCapsule`，提交和推送请在此目录操作。外层 `D:\ly\旅行青蛙` 保留本地备份、生成工具与检查文件。GitHub Pages 使用 `main` 分支的根目录 `/ (root)`；根目录 `index.html` 自动进入 `viewer/index.html`。

## 数据是怎样获取的

### 1. 从自己的 Android 手机复制游戏分享图片

通过 USB 连接手机，在本人授权 USB 调试后，使用 **ADB + Python 本地备份工具**读取游戏专属外部目录中可访问的分享图片：

```text
/sdcard/Android/data/com.aligames.lxqw.hhb/files/umeng_cache/
```

只复制经过筛选的游戏图片，记录来源路径、可取得的文件时间、文件大小和 SHA-256。图片传输前后进行校验，保留原始文件，不对原图重新编码或有损压缩。

完全相同的图片通过 SHA-256 和逐字节比较去重，只保留一份原图，同时保留每份副本的来源记录。画面相似但日期、文字、边框或文件内容不同的图片分别保存。

### 2. 在电脑上识别、核对图片信息

使用 **Windows 本地 OCR**识别图片中的文字，再结合图片进行分类和可见信息核对。整理内容包括图片上的日期、地点、署名、物品名称和说明；OCR 原文与整理后的字段分开保存，便于追溯。

图片日期仅表示图片中印出的日期，没有被当作真实出发时间、获得时间或服务器记录。账号名“土特产代购”和用户 ID `4345023` 后续由本人明确提供并确认，保存在 `parsed/profile.json`，不是根据 OCR 推断或从私有存档读取。

### 3. 从 MIUI 系统备份中整理通用素材

在手机上使用 **MIUI 系统本地备份**备份目标游戏，再通过电脑只读检查备份目录，按需读取其中未加密的静态图片和图集索引。使用 **Python + Pillow**按照图集坐标裁切，导出无损 PNG，并保留图集原件、坐标、资源编号及校验信息。

这些素材构成页面中的“图鉴、物品、家具”。有个人分享图可以对应的资源，补充图片中可确认的名称、说明或关联入口；其余使用原始资源编号。

通用资源只说明游戏包含这些素材，**不代表我的账号已经拥有或解锁它们**。系统备份整包及本地检查工作文件不随网页发布。

### 4. 还原不倒翁静态图片

在本人追加授权后，从同一 MIUI 备份的安装包中按清单读取 58 个不倒翁静态图片文件，并在电脑上使用客户端兼容的 **XXTEA 图片解码方法**还原为透明 PNG。逐张验证 PNG 完整性、尺寸和透明通道，记录解码前后的 SHA-256，保留原始密文和本地处理记录。

解码对象仅为通用图片素材，没有读取账号运行时参数、登录信息或支付数据。网页中只包含已转换的图片和展示资料，不包含图片密钥、客户端解密代码或系统备份整包。通用配置包仍未解密。

### 5. 生成本地 HTML 档案

将原图、整理后的 JSON 信息和资源图片组织为静态 HTML 页面。页面无需后端、数据库或外部脚本，本地打开时不依赖网络。备份、OCR 和整理过程在本地完成；GitHub Pages 用于展示另外发布的网页与图片。

## 目前保存了什么

以下为 2026 年 9 月 9 日整理的这一批数据，后续补充时可能变化。

### 个人游戏图片

共保存 **246 张去重图片**，对应 **485 条原始来源记录**。

| 内容 | 数量 |
| --- | ---: |
| 明信片 | 176 张 |
| 本人拥有的不倒翁分享图 | 40 张 |
| 贴纸分享 | 12 张 |
| 活动图片 | 7 张 |
| 收藏图片 | 4 张 |
| 道具图片 | 3 张 |
| 旅行记录图片 | 2 张 |
| 扭蛋记录图片 | 1 张 |
| 其他图片 | 1 张 |

其中 **219 张记录了图片上的日期，49 张记录了地点标注**。这些数量可以重叠，也不等同于实际旅行次数或到访地点数量。原有的 176 张分享图片及收藏类缓存现在统一放在“其他图片”；“旅行汇总”来自已有图片，尚不是完整旅行日志。当前尚未导入人工补充截图。

### 明信片静态资源与地点文字

左侧“明信片”分为“节日 / 特殊”“目的地”“常规”三个子栏。“目的地”内部再按“全部、北京、成都、重庆……”使用上方可换行子栏筛选，并可继续选择青蛙单人、青蛙与壁虎、刺猬或萤火虫的组合。当前从客户端的 `PictureTag` 有效清单中恢复 **144 个可展示地点**，按 `Picture` 配置记录的角色坐标生成 **564 张目的地组合图**；例如重庆包含“重庆·沙坪坝·磁器口”“重庆·南岸区·铜元局站”“重庆·宁厂古镇”。另保留 **171 张节日或特殊图片**。

“常规”收录 **19 类、39 组场景和 256 个原始图层**，采用与“不倒翁”一致的拼装界面：左侧预览，右侧按场景、角色、背景、前景选择，选择图层后可在下方调整横向、纵向、大小、旋转和翻转；画布下方提供撤销、重做和复位，素材分类栏最右侧提供随机组合与保存 PNG，窄屏自动换行。保存结果为 1000 × 700 无损 PNG。蓝花藤和两种风铃木已依据可见脚底及承托物人工校准默认位置，校准值单独保存在 `parsed/normal-postcard-layouts.json`，不改写原始配置，也不代表已还原服务器坐标。

在 `Unique` 类中补充找到猫版垂丝海棠 **3202** 和三组木棉花绣眼鸟 **3203–3205**，同样放入“常规”的对应分类。猫版按参考图固定猫和角色，青蛙站在猫背上，随机组合只变化花朵；无猫海棠为 202–204，配置分别提供中、左、右三个角色组合位置，仍可手动调整。木棉花三组带鸟树干和前景花枝均已加入。猫和上方花朵虽在同一 PNG 中，绘制时分区读取，移动花朵不会带动猫，原始文件不修改。补充配置保留真实图片 ID、原配置和资源映射；具体构图结合参考图校准，尚非取得服务器最终照片坐标，也不代表本人账号已拥有这些照片。

地点正式文字、图片 ID、内部名称、地区、角色组合、每层坐标、文件大小和 SHA-256 保存在 `parsed/goal-postcards.json`，图片分别保存在 `media/goal-postcards/` 与 `media/special-postcards/`。这些数据来自本人 MIUI 备份中的安装包及热更新静态资源，并用客户端自带的 XXTEA EAB 解码逻辑离线读取配置；过程未联网，也未读取登录凭证、Cookie、Token 或设备密钥。

静态资源存在不等于本人账号已经拥有。当前仍未取得完整的账号相册拥有列表。常规场景的角色坐标来自客户端配置，背景与前景的最终随机坐标来自服务器返回，本地静态配置没有保存，因此生成器提供的是代表性组合，不冒充账号实际收到的明信片。界面规范记录在 `VIEWER-UI-STANDARD.md`。

### 通用资源图鉴

原图集包含 **451 项可见素材**，与上述个人图片分别统计。当前展示合并友情绘本的 22 件藏品，原图集中的两条藏品入口指向共用记录，共 **471 项**，不重复复制图片。

| 类别 | 已匹配素材 | 已拥有 | 未拥有 |
| --- | ---: | ---: | ---: |
| 图鉴：纪念品 | 64 | 43 | 21 |
| 图鉴：特产 | 66 | 64 | 2 |
| 图鉴：藏品 | 22 | 18 | 4 |
| 物品 | 175 | 待确认 | 待确认 |
| 家具 | 144 | 待确认 | 待确认 |

资源资料包含原始编号、图片尺寸、资源版本、来源图集、裁切坐标、SHA-256，以及已有证据支持的名称、说明和关联分享图。透明占位图不展示，完全相同的导出图片共用同一文件。

图鉴分类根据本人提供的六张游戏截图与补充确认整理。博物馆器物、小画架归纪念品，金色鸟形冰棍归特产。按本人确认，本批图鉴中截图未出现的素材标记为未拥有；此规则不用于物品、家具或未来新增素材。分类、获得情况与截图文件名记录在 `parsed/catalog-annotations.json`，藏品在两个入口共用 `parsed/friendship-ownership.json`。

截图中的水果、蔬菜、乳制品等 24 项特产已从 `icon1_sheet` 图集中找到并无损裁切，另发现一项截图中没有的樱桃。按本人确认的规则，24 项标为已拥有，樱桃标为未拥有。界面名称按图片内容作档案描述，不冒充尚未取得的游戏正式名称。原始图集、坐标索引和分类依据均保留。

### 不倒翁部件与自由拼装

“不倒翁”默认进入“我的收藏”，另一个标签为“自由拼装”。从本人新生成的 80 份缓存中按 SHA-256 去重，保留 **40 张完整分享原图**，每张合并两份来源，均由本人确认账号拥有。小图为另外生成的白底局部无损 PNG，点击查看完整缓存原图，支持编号搜索、排序、翻页和单独打开原图。图片日期为 2026-09-09，不代表获得日期；展示编号不是游戏物品 ID。

原图保存在 `raw/`，小图保存在 `media/tumbler-owned/`，拥有记录、小图裁切坐标、来源和校验信息保存在 `parsed/tumbler-owned.json`。这批图片反映本人确认的 40 款，不表示已取得账号全部不倒翁记录。

另保存 **58 张不倒翁透明 PNG**：12 张底图、12 张配套遮罩、23 张眼睛、8 张装饰、2 张底部和 1 张中部。这些是组成部件，不是 58 款不倒翁，也不计入前述 426 项资源图鉴或 246 张个人图片。

在“不倒翁”页面可以切换部件、调整位置和大小、旋转及翻转、改变底色、随机组合、撤销重做，并保存 800 × 800 透明 PNG。底图自动匹配同编号遮罩；组合保存在当前浏览器的本地存储中，存储不可用时会提示。拼装结果是独立创作，不代表我的账号实际拥有或在游戏中保存过这一组合。

原始尺寸的还原图片在 `media/tumbler/`，来源和 SHA-256 在 `parsed/tumbler-catalog.json`。生成 HTML 时校验全部 58 张图片，再将它们嵌入页面，使离线打开时也能正常导出组合图片。

### 友情绘本

从本人 MIUI 系统备份的普通静态图片中，另保存 **96 个友情绘本相关图片条目**：21 张绘本页、22 张赠礼图片、53 张场景与界面素材。原始 PNG 按字节保留并校验 SHA-256，来源优先采用备份中已有的热更新图片，缺少的从同一备份的安装包读取。完全相同的图片内容共用一份原件。

左侧“友情绘本”进入选书页，以 `back_picture_book` 为底图，使用 `book_wugui`（困困）、`book_maotouying`（胖胖）、`book_songshu`（跳跳）作为可点击封面。点击后进入对应作者的绘本，可搜索资源编号、放大、前后翻页和返回选书；赠礼图片、场景与界面素材仍可分类浏览。困困编号 1–7、胖胖为 100 段、跳跳为 200 段，当前各 7 页。图片保存在 `media/friendship/`，作者、分类、尺寸、来源版本和 SHA-256 保存在 `parsed/friendship-catalog.json`，本人确认的编号归属另存于 `parsed/friendship-authors.json`。

本人确认已获得 **19 / 21 页**：困困 6/7（编号 7 未获得）、胖胖 6/7（编号 106 未获得）、跳跳 7/7。获得情况单独保存在 `parsed/friendship-ownership.json`，来源为本人补充，不是从私有存档中读取；未获得的两页仍保留静态资源图片并明确标注。

友情绘本原“赠礼图片”现名“藏品”。本人确认拥有 **18 / 22 件**：编号 1、3、5、202 未拥有，其他当前已归档藏品均拥有。对应 `draw_icon_*` 的状态单独写入同一文件的 `gifts` 字段，不影响绘本页状态，也不推断未来新增藏品。

静态程序的 `DrawingModel` 表明个人记录由 `guest_load_drawing` 加载，包含 `pages`、`colls` 等字段；本次只确认了字段和加载逻辑，没有请求或读取账号响应。正式标题和故事文字仍未取得。三个通用配置入口 `drawingPageData`、`drawingCollectData`、`drawingCommonData` 指向 `config.eab`，本次未解密该配置。作者归属、绘本和藏品获得状态来自本人确认；编号排序不代表已确认的故事顺序。

### 基本信息与来源记录

左侧“称号”以纯文字展示本人提供的 8 张游戏截图中可读的 **64 个已拥有称号**，按截图时间和图内顺序排列，支持搜索。名称与截图文件名、SHA-256 保存在 `parsed/titles.json`；问号占位项未录入，不推断获得时间或当前装备称号。核对截图留在电脑的本地工作目录，不随称号页面展示。

- 游戏：《旅行青蛙·中国之旅》，安装版本 `1.0.20`。
- Android 包名：`com.aligames.lxqw.hhb`。
- 本次设备系统：Android 14；设备型号标识：`23049RAD8C`。
- 备份日期、原始文件清单、文件大小、来源路径、文件时间和 SHA-256。
- 图片分类、OCR 原文、核对后的可见信息及相关整理记录。

## 尚未取得的内容

目前没有取得完整明信片拥有列表、特产与家具解锁状态、道具及货币余额、任务、活动进度和完整旅行历史。称号已根据本人截图整理为文字，尚无称号获得时间和数据库记录。现有图片可以保留部分可见内容，但不能据此确认账号数据的完整性，也不能断言缺失信息一定只存在服务器端。

不倒翁的通用图片已还原，另有本人确认拥有的 40 款分享图；尚未取得完整账号清单或存档中的配色和摆放参数。相关通用配置指向加密的 `config.eab`，本次没有解密该配置；拼装页依据已有图片与客户端居中、裁切逻辑实现，不宣称完整复现官方编辑器的规则。

后续可以用游戏内保存图片或系统截图的方式补充可见页面，记录每项信息的图片依据。

## 文件组织

```text
GameTimeCapsule/
  viewer/         离线 HTML 页面
  raw/            去重后保留的个人游戏原图
  media/          通用资源图集、索引、裁切图片和不倒翁部件
  parsed/         图片信息、OCR 原文、分类资料和资源清单
  screenshots/    后续人工补充的游戏截图
  reports/        备份、整理及完整性检查记录
  manifest.json   原始文件与来源清单
```

网页已隐藏数据地图、原件校验视图及侧栏中的备份报告入口，相关文件仍保留用于核对。隐藏入口不等于文件不可访问；公开仓库中已提交的文件仍可通过仓库或对应地址查看。

## 备份原则

仅整理本人设备和本人账号相关数据。未使用 Root、越狱、进程注入、Hook、系统沙盒绕过或网络抓包；未读取或导出登录密码、Cookie、Token、支付凭证和设备密钥。不倒翁静态图片的本地解码经过本人追加授权，范围限于上述通用素材。遇到无法在允许范围内读取的内容，记录限制，不把未知数据编造成已保存内容。

原始图片与来源记录用于个人留存和核对。游戏名称及游戏画面、资源素材的相关权利归各自权利人所有；本项目为个人纪念档案，与游戏官方无关。
